buildscript {
    repositories {
        jcenter()
    }

    dependencies {
        classpath 'net.idlestate:gradle-download-dependencies-plugin:1.+'
        //classpath "io.spring.gradle:dependency-management-plugin:1.0.11.RELEASE"
        //classpath "au.com.dius.pact.provider:gradle:4.3.5"
    }
}

apply plugin: 'net.idlestate.download-dependencies'
apply plugin: 'java'
//apply plugin: 'au.com.dius.pact'

group 'org.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
//    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.0'
//    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.0'

    //Pact
    //testImplementation 'au.com.dius.pact.consumer:junit5:4.3.5'
}

test {
    useJUnitPlatform()
}

downloadDependencies {
    localRepository = file( 'myLocalGradleCache' )
}